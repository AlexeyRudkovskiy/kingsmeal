{% extends 'admin.html.twig' %}

{% block title %}{{ 'Editing Photo :: KingsMeal' | trans }}{% endblock %}

{% block body %}

    <div class="grid">
        <div class="column column-4 form-column">
            {{ include('photo/_form.html.twig', {'button_label': 'Update'}) }}
        </div>
        <div class="column column-8">
            <div>
                <img src="{{ photo.photoUrl }}" class="photo-preview" />
            </div>
        </div>
    </div>

{% endblock %}
