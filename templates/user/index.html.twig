{% extends 'admin.html.twig' %}

{% block title %}User index{% endblock %}

{% block body %}
    <table class="table">
        <thead>
            <tr>
                <th class="id-column">Id</th>
                <th>Username</th>
                <th style="width: 200px;" class="pull-right">Roles</th>
                <th class="pull-right actions-column">actions</th>
            </tr>
        </thead>
        <tbody>
        {% for user in users %}
            <tr>
                <td>{{ user.id }}</td>
                <td>{{ user.username }}</td>
                <td class="pull-right">{{ user.roles ? user.roles|join(', ') : '' }}</td>
                <td class="pull-right">
                    <div class="buttons-group button-group-pull-right">
                        <a href="{{ path('user_show', {'id': user.id}) }}" class="button button-small">show</a>
                        <a href="{{ path('user_edit', {'id': user.id}) }}" class="button button-small">edit</a>
                    </div>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="4">
                    <div class="alert">
                        <div class="alert-message">Не найдено ни одного пользователя</div>
                        <div class="alert-actions">
                            <a href="{{ url('user_new') }}" class="button"><span class="icon">📝</span><span>Создать первого пользователя</span></a>
                        </div>
                    </div>
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>
{% endblock %}
